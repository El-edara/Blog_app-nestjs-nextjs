{
  "version": 2,
  "builds": [
    {
      "src": "apps/api/main.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "apps/api/main.ts",
      "methods": ["GET", "POST", "PUT", "DELETE"]
    }
  ]
}
